app:
  # seconds to record per utterance (you can set 8-20)
  record_seconds: 12
  language: "hi"   # whisper language tag; keep "hi" or "en" - whisper handles code-mixed input well

models:
  whisper_model: "tiny"   # tiny | base | small | medium | large
  llm_local: "google/flan-t5-small"  # small, change if you use hosted HF Inference

tts:
  engine: "pyttsx3"  # pyttsx3 or coqui
